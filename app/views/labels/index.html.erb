<div class="container px-4 py-8 mx-auto">
  <h1 class="text-2xl font-bold mb-6 text-center text-[#2D3047]">Etiquetas de Confirmação</h1>
  <div class="p-6 mb-8 overflow-hidden bg-white rounded-lg shadow">
    <p class="mb-4">Gere etiquetas de confirmação para todas as <%= Family.count %> famílias cadastradas.</p>
    <p class="mb-2">Cada etiqueta terá:</p>
    <ul class="pl-8 mb-4 list-disc">
      <li>Tamanho: 5cm x 5cm</li>
      <li>Nome da família</li>
      <li>Instruções para confirmação</li>
      <li>O código de confirmação</li>
    </ul>
    <div class="flex justify-center mt-6">
      <%= link_to "Gerar PDF de Etiquetas", generate_labels_path(format: :pdf),
                  class: "px-6 py-3 bg-[#73C8A9] text-white font-medium rounded-md hover:bg-[#73C8A9]/90 transition-colors" %>
    </div>
  </div>
  <div class="overflow-hidden bg-white rounded-lg shadow">
    <div class="px-4 py-5 sm:px-6">
      <h2 class="text-lg font-medium text-[#2D3047]">Lista de Famílias</h2>
    </div>
    <div class="border-t border-gray-200">
      <ul class="divide-y divide-gray-200">
        <% @families.each do |family| %>
          <li class="px-4 py-4 sm:px-6">
            <div class="flex items-center justify-between">
              <p class="text-sm font-medium text-[#2D3047]"><%= family.name %></p>
              <p class="text-sm text-gray-500"><%= family.token %></p>
            </div>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
